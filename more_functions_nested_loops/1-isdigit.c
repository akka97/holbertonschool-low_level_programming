#include "main.h"
/**
* _isdigit - check for a digit
* @c: integer parameter
* Return: One in case a digit, otherwise zero.
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
return(0);
}
