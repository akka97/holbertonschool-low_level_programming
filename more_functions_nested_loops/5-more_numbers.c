#include "main.h"
/**
* _isupper - check for uppercase character
* @c: integer parameter
* Return: Always one, otherwise zero.
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

return (0);
}
