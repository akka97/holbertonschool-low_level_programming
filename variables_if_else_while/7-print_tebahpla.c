#include <stdio.h>

/**
 * main - Prints alphabet in lower and upper case
 * Used char only three times
 * Return: Always 0.
 */

int main(void)
{
	char letter;

	for (letter = 'z'; num >= 'a'; letter--)

		putchar(letter);

	putchar('\n');

	return (0);
}
