#include "main.h"
/**
 * _abs- computes the absolute value of an integer
 * @i: positive number
 * Return: zero
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = -1 * i;
		return (i);
	}
	else
	{
		return (i);
	}
}
